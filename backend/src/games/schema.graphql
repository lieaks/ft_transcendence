# import User from "../users/schema.graphql"
scalar DateTime

type Game {
	id: String!
	players: [User!]
	winner: User
	looser: User
	createdAt: DateTime!
	finishedAt: DateTime
}

type Query {
	game(id: String!): Game
}

type Mutation {
	createGame(userId: String!, enemyUserId: String!): Game!
}
